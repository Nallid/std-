#pragma once
#include <iostream>

template <typename m_type>
class Array {
private:
	unsigned int m_size;
	
public:
	Array < m_type >(unsigned int size) : m_size(size) {}

	Array < m_type >() {
		
	}
};